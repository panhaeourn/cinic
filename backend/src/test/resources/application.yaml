spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-google-client-id
            client-secret: test-google-client-secret
            scope:
              - profile
              - email
  datasource:
    url: jdbc:h2:mem:clinic_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

app:
  frontend-url: http://localhost:5173
  security:
    allowed-origins: ${APP_ALLOWED_ORIGINS:http://localhost:5173}
    admin-google-email: panhaeourn7@gmail.com
    jwt-secret-base64: ${APP_JWT_SECRET_BASE64:Y2hhbmdlLXRoaXMtand0LXNlY3JldC1pbi1lbnYtYmVmb3JlLXByb2R1Y3Rpb24tMjAyNg==}
    access-token-ttl-minutes: 30
    refresh-token-ttl-days: 7
